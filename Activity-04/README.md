# Money Converter

A simple C++ program that converts currencies between Philippine Peso (PHP), US Dollar (USD), and Euro (EUR).

## Concepts Practiced

- `#include <iostream>` → Allows the program to use input/output features.
- `using namespace std;` → Lets you use `cout` and `cin` instead of writing `std::cout` and `std::cin`.
- `int main()` → The starting point of every C++ program.
- Functions → Used to organize different currency conversion calculations.
- Parameters → Passes values into functions for processing.
- `switch statement` → Handles different conversion options selected by the user.
- Mathematical operations → Used to calculate currency conversions.
- `return 0;` → Ends the program successfully.

## Features

- PHP to USD conversion
- USD to PHP conversion
- PHP to EUR conversion
- EUR to PHP conversion
- USD to EUR conversion
- EUR to USD conversion
- Menu-based user interaction

## Technologies Used

- C++
- Standard Input/Output Library (`iostream`)
